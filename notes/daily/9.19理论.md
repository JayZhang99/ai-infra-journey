1. reduce_sum 正向 [2,3] → []，反向为什么必须 [] → [2,3]？

   因为backward 计算的是梯度，必须跟输入一致，所以也得是[2,3]

• 2. 从 Python load() 到 CUDA kernel 执行，完整链路有哪些层？

​	 compiler 编译 生成.o -> 链接生成 shard library ->



• 3. cold、warm、loaded 分别比较什么？

​	cold 无缓存 无加载结果

​	warm 有缓存 有加载结果

​	loaded 无缓存 有加载结果

• 4. ExtensionVersioner 与 Ninja 各自决定什么？

​	ExtensionVersioner决定 

​	Ninja 决定编译选项

• 5. API 与 ABI 的根本区别是什么？

​	API 是接口层的抽象

​	ABI 是设备层的

• 6. torch.version.cuda、nvcc、nvidia-smi 各代表什么？

​	torch.version.cuda pytorch cuda的版本

​	nvcc 解释器版本

​	Nvidia-smi driver 底层驱动版本

• 7. nm、ldd/readelf、Dispatcher 表各能证明什么？

​	nm 证明接口声明与定义

​	ldd 证明链接

​	Dispather 证明python 注册

• 8. 证明 warm build，至少应固定并记录哪些变量？

​	固定并记录 产物sha256 产物修改时间 编译选项





1. 为什么 cold build 成功不能证明 CUDA Dispatcher 路由正确？

   cold build 成功只能证明编译没问题，不能证明dispather的路由是预期路由

• 2. warm 与 loaded 的关键区别是什么？

​	有没有进程缓存

• 3. flag 触发重编译，为什么 SHA 仍可能不变？

​	源码中可能没有用到改变的flag

• 4. ldd 显示 libtorch_cpu.so not found，实际 load 为什么仍可能成

功？

​	ldd只表示在当前会话找不到动态库

• 5. DT_NEEDED 能证明什么、不能证明什么？



• 6. nm -D -C 中 T 与 U 分别代表什么？

​	T 函数定义

​	U 函数声明，无定义

• 7. Meta 从 fallback 变 direct 说明了什么？

​	说明加载了kernel

• 8. 为什么 Mac 与 WSL 的 Dispatcher 断言不同？

​	一个只有cpu。一个有CUDA

• 9. Manifest 为什么既记录 revision 又记录 dirty？

​	同样的revison 有dirty也是不同的

• 10. cold 与 warm 至少保持哪 6 项相同？

​	flags CUDA NVCC GPU kernel 

• 11. 为什么不对整张 Dispatcher 表做 golden snapshot？

​	整张Dispatcher表太大了

• 12. 写一条关于 warm build 的 Observation/Hypothesis/Next test。

​	Observation ： Ninja no work to do， SHA 256 mtime 相同

​	Hypothesis 因为已有缓存，没有改变编译选项所以不用重编译	



​	Next test ，更改flag 重新build 有Ninja编译输出 SHA256 和mtime改变