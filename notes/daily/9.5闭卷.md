1. 4 分：对比 Static、Dynamic、Continuous Batching。

   静态 Batching ，固定的Batching 大小，可能导致长Batch等待从而导致

   TTFT 变差

   Dynamic Batching ，动态Batching，但是可能导致长请求长时间不响应

   Continuous Batching，批处理Batching，可能导致额外的读写开销和调度开销

2. 4 分：写一次 iteration 的六步状态推进。

   

3. 4 分：解释 KV block、内部碎片与准入控制。

   KV block 大，内部碎片多

   KV block小，内部碎片小，但控制开销大

4. 4 分：为什么长 Prefill 会使 Decode ITL p99 变差？

   因为长 Prefill 会占用更多的Token， 导致Decode 可用token变少，

   ITL劣化

5. 4 分：tokens/s 上升但 ITL 变差，写两个假设和实验。

   1. Prmompt 变长，Profile变长
   2. Hkv变大





1. 4 分：画请求到首 token 的时间线，并写 TTFT 分解。

   request arrives -> parse/tokenize -> queue wait

   -> batch -> H2D -> Prefill -> Fist Token -> decode loop

   -> network

   TTFT 是从request arrives到First Token的时间段

2. 4 分：解释 Prefill 与 Decode 的输入、状态和性能倾向。

   Prefill T 为全T， Decode为1

   Profile 性能更多用于并行计算

   Decode更多用于存取数据

3. 4 分：区分 TPOT 与 ITL，并写 E2E 近似公式。

   TPOT 是Token维度，服务端输出速率

   ITL 是每个Iteration之间的间隔，还包括上下游动作

4. 4 分：区分 QPS、tokens/s 和 Concurrency。

   QPS 是衡量 处理请求速度

   token/s 完全是在模型处理速度放慢

   Concurrency 是等待进行中的任务

5. 4 分：TTFT p99 高、TPOT 稳定，提出两项控制实验。

   1. 减少prompt 长度
   2. 增多Hkv