1. 4 分：画 pageable 与 pinned H2D 路径，并解释 DMA。

   pageable -> cpu -> GPU -> device

   Pinned memory -> DMA -> device

   

   DMA即可以直接进行CPU内存和GPU内存通信的硬件

   

2. 4 分：解释 non_blocking 返回、copy 完成和 overlap 的区别。

   non_blocking返回只能说明CPU指令提交

   copy 完成是指数据传输动作完成

   overlap需要流水线，即 COPY 和 Compute在不同Stream并行

3. 4 分：说明同 Stream 为何串行，不同 Stream 为何仍不保证 overlap。

   同Stream一定按照顺序执行

   不同Stream 还必须有pinned memory，non_blocking = True

   没有item 等同步操作

4. 4 分：写 sequential 与 pipeline 理想时间公式，并解释 chunk

tradeoff。

​	sequential = n * (compute time + copy time)

​	pipeline = n* max(compute time, copytime) + min(copytime, compute time)

​	

5. 4 分：写 Event 与 Profiler 的职责，以及证明 overlap 的三条 trace 证

据。

​	event实现Stream同步，Profiler抓取各动作耗时

​	根据9.3_light_trace.json结果，好像并没有实现pipeline





1. 3 分：用 shape 解释 Dmodel/H，并与 Mask 广播规则对比。

   从 [B,T Dmodel] 转为 [B,H, T, Dh]

   Mask 是[1,1,T,T] 广播到 [B,H,T,T]

2. 3 分：从 masked scores 写到全 False 行产生 NaN 的数值链。

   -inf-(-inf) = NaN

3. 4 分：写三阶段 Q/K/V shape、历史是否重算、Cache 如何变化。

   prefill QKV都是[B,H,T,Dh] ，一次算全Cache

   Uncached 	QKV都是[B,H,T,Dh], 每次重算 ，每次Cache T+1

   cached  Q[B,H,1,Dh] K,V 是[B,H,T,Dh], 不重新算，每次Cache T+1