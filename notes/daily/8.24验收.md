8.24 验收题：20 分

2 分：Parameter 与 persistent Buffer 在注册、保存和优化方面有何不

同？

​	Parameter自动注册 persistent buffer需要注册

​	都保存在running_stats

​	优化 Parameter 优化器step优化，persistent buffer 非特殊不自动优化

2 分：为什么 eval 不能关闭 Autograd？

​	因为只将model模式改为非训练

2 分：no_grad 下 BatchNorm running stats 为什么仍可能变化？

​	因为只是不算梯度,running stats仍可能Batch norm变化

2 分：grad_fn 与 .grad 分别是什么？

​	形成梯度的函数和梯度值

2 分：retain_graph 和 create_graph 各解决什么问题？

​	retain_graph 解决需要累积多次梯度

​	create_graph解决需要多阶导数

2 分：非标量 backward 为什么需要 gradient 参数？

​	需要方向计算梯度

2 分：CUDA 的 Python 返回时间为什么可能不是 GPU latency？

​	因为CUDA是异步的

2 分：Warmup 为什么不进入稳态统计？

​	因为可能有额外开销

2 分：compute-only 与 end-to-end 有何不同？

​	compute-only 只关心计算开销

​	end-to-end则端到端关心所有额外开销

2 分：为什么性能单测不应断言固定毫秒阈值？

​	不一定CPU和GPU 谁快谁慢，且毫秒阈值受硬件、软件影响



边界、统计与 HVP：15 分

3 分：样本 [1,1,1,1,6] 的 mean、median、max 是多少？它说明什么？

​	mean = 2， median = 1, max= 6 ,说明 mean 和 median 都不能完全描述样本分布

3 分：M=N=K=512，估算一次 MatMul 的 FLOPs。



3 分：CUDA perf_counter 显示 0.004 ms，但同步后是 0.2 ms，解释

差异并给出正确方案。

​	perf_counter 指的是提交kernel的时间，

​	正确应该用event来计算

3 分：H=[[2,3],[3,4]]，v=[1,-1]，求 Hv 与 v^T Hv。

​	Hv = [-1,-1]

​	v^T Hv = 0

3 分：一个结果包含 H2D，另一个只测 a@b，能否计算 GPU 加速比

？为什么？

​	不能，因为第二个结果没有包含H2D开销