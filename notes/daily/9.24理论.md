1. NodeProto 如何连接另一个节点？TensorProto 与 ValueInfoProto 各保存什么？

   通过NodeName 连接

   TensorProto 保存dtype device 

   ValueInfoProto 保存数值

• 2. available、selected、supported、assigned 分别由什么证据回答？

​	available torch.is_available

​	selected

​	supported

​	assigned 根据profiler 结果的name 和 provider 回答哦

• 3. 为什么 CPU fallback 是跨设备 copy 的可能原因，而不是结果？

​	因为出现fallback -> 导致恳呢哥出现数据边界，可能导致跨设备copy

• 4. 普通 NumPy CUDA Run 与 GPU resident Run 的 latency 边界有什么不同？

​	普通的CUDA RUN 是E2E 可能包含 H2D 和 D2H

​	GPU resident Run，即纯GPU latency

• 5. 外部 CUDA pointer 的七项合同是什么？

​	dtype

​	device

​	lifetime

​	shape



• 6. code fingerprint 与 worktree audit 为什么必须分离？

​	不一定源码变动 worktree就会变动

• 7. 为什么同一 Git HEAD 仍可能对应不同执行源码？

​	git diff不同

• 8. schema v1/v2 混合为什么必须拒绝？

​	合同不同，没办法比较

• 9. require_clean_source 为什么应在计时前执行？

​	提早结束避免资源浪费

• 10. 可复现实验为什么不要求每个 latency sample 完全一样？

​	还可能受环境，硬件的影响