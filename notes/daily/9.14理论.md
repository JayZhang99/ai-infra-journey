1. [2] rows=1000, threads=256：写 Baseline/Block 的 row 和 grid.x。

   Baseline row= threads  grid.x = (1000+255)/256 = 4

   Block row = blocks grid.x = 1000

2. [2] 用“写者 -> Barrier -> 读者”解释跨 Warp Reduction。

   Warp Reduction

   写者 各warp lane[0] 读者 各warp

   写者  warp[0] lane[0] 读者 block

3. [2] Naive Transpose 哪个方向不合并？用 lane 地址说明。

   写方向不合并， 读的时候列连续，所以合并，写的时候列差stride

4. [2] tile[32][32] 列读为何冲突？33 如何改变 Bank？

   [1][0] [2][0] ... 都读bank0

   32-way bank-conflict

   tile33的话，同一列不同行不映射到同一bank

5. [2] 定义 Transpose p95 的样本，并写 effective GB/s 公式。

   Transpose p95 是每一batch 95% 用时

   effective GB/s = 2 * height * width * data_bytes / 1e6

6. [2] 画出 Python -> Dispatcher -> CPU C++ Kernel 调用链。

   Python 调用接口 -> Dispatcher找到实现 ->CPU Kernel实现后返回结果

7. [2] 分别说明 TensorImpl 和 Storage 的职责。

   TensorImpl 说明 type ,device, layout

   Storage 说明数据保存位置

8. [2] TORCH_LIBRARY 与 TORCH_LIBRARY_IMPL 有什么不同？

   TORCH_LIBRARY 是说明， TORCH_LIBRARY_IMPL 是实现

9. [2] 为何 data_ptr<float>() 前必须验证 dtype/contiguous？

   不验证就会以预期dtype 和layout 解析数据，可能导致不可知错误

10. [2] opcheck、assert_close、gradcheck 各验证什么？

    Assert_close 检查数值