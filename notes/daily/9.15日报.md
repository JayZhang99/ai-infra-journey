1. 理论：定向 _7_/12，新课 _5_/10；具体错题编号。

   跨 Warp Reduction0.5/2写者、槽位和读者混淆50×100 p95、GB/s0.5/2理解统计层级，但写成 p96，GB/s 括号错误TensorImpl/Storage/view1/2基本方向正确，view 部分不准确Schema/CPU/CUDA IMPL1/2理解有多个实现，但误认为 Schema 选择 Backend

   同一 Operator 选择 Backend1.5/2知道由 Dispatcher 选择，但未说明 Tensor KeySetCUDAGuard1.5/2知道解决多 GPU 问题，但缺少 current device 机制Current Stream1/2只答“保证顺序”，未说明生产者—算子—消费者依赖Launch Check1/2知道检查启动参数，但未区分异步执行错误pytest 找不到 Operator0/2原因不是 CUDA 未实现，而是注册是进程内状态

2. CPU build 到达 compile/link/load/smoke 哪一级？完整末行是什

么？

​	smoke,tensor([4., 7.])

3. WSL combined build 的 torch/CUDA/NVCC/driver/git 是什么？
4. Dispatch 表是否同时存在 CPU/CUDA？CPU/CUDA 输出 device

分别是什么？



5. pytest collected/passed/skipped/failed；失败属于哪一层？

   MAC:11 passed, 6 skipped

   WSL: 17 passed

6. Transpose summary 是否落盘？输入是否正好 5 轮 × 16 cases？

   Done

7. 今日明确未支持：Autograd、Fake/Meta、FP16 中哪些仍未做？

   都没有做