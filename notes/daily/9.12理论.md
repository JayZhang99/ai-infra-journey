1. 画出 2 Blocks x 4 Threads、n=18 的完整分工。

   Thread 0 分配 n = 0, 8,16

   Thread 1 分配 n = 1,9,17

   Thread 2 - 7 分配 n = 2-7 8-15

2. 解释为什么 input[8] 不等于 global thread 8。

   Input[8] 是指input数据结构中的第八个数据

   和grid中 thread8没有必然联系，有没有thread 8 都是不确定的

3. 写出 Baseline 与 Block 的 row 和 Grid 公式。

   Baseline row数量等于Thread 数量 ,Grid launch = rows / threads

   Block row 数量等于 block数量 ，Grid launch = row 

4. 指出四个 barrier 分别保护哪次写后读。

   max 第一个 barrier 保护计算wrap 局部最大值

   第二个barrier 保护计算所有wrap中的最大值

   sum 第一个 barrier 保护计算wrap局部和

   第二个barrier保护计算算有wrap的和

5. 解释两个单位元为何不能互换。

   sum 的单位元是0

   max 的单位元是 -inf

   互换之后，单位元会影响结果



1. 2 分：区分 local_tid、global_tid、data_index。

   local_tid 是在block里的线程id 范围是 0 - blockDim

   global_tid 是在 grid 里的线程id 范围是 0 - threads -1

   data_index 是数据下标，和前面两个线程id没关系

2. 2 分：解释 n 为什么不是 Grid 总线程数。

   n是数据总数，和Grid 总线程数无强相关

3. 2 分：写出 Grid-stride 的初始值与步长。

   Grid-stride 初始值 BlockId * block Dim + thread id

   步长 GridDim * BlockDim

4. 2 分：写出 Baseline 和 Block 的 row 映射。

   Baseline 一个线程处理一行， rowro

5. 2 分：说明无数据线程为何不能在 barrier 前 return。