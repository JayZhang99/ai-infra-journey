\## 2026-09-02 Evidence

Theory:

\- retest score: 12.5

\- remaining errors:

| 题目                     | 得分  | 评价                                                         |
| ------------------------ | ----- | ------------------------------------------------------------ |
| 1. Pre-Norm 与 Residual  | 2.5/3 | 两条公式正确，但没有明确指出 Residual 主路是绕过子层的 identity 路径 `x → +` 和 `h → +`。 |
| 2. SwiGLU shape          | 3/3   | 三组权重及主要 Activation shape 正确。                       |
| 3. `Dmodel % H` 与广播   | 0.5/3 | 只描述了大致目的，没有回答整除条件和广播规则。               |
| 4. 全 False Mask         | 1.5/3 | “不存在和为 1 的分布”结论正确，但缺少 `-inf`、分母为 0 和 NaN 的计算过程。 |
| 5. 三类 Attention 数据流 | 1/4   | cached Decode 的 Query shape 写对了，但 Prefill、no-cache 和 Cache 读写过程基本没有展开。 |
| 6. KV Cache bytes        | 4/4   | 公式和计算完全正确，结果为 `536,870,912 bytes = 512 MiB`。   |

Test discovery:

\- collected before:  13

\- collected after: 16

\- targeted: 16

\- full repo: done 

Benchmark:

\- boundary:done

\- device: done 

\- command: python python/benchmark_attention.py --device cpu --sequences 128 512 --batch 1 --heads 8 --kv-heads 8 --head-d\
im 64 --dtype float32 --warmup 5 --repeats 20 --output benchmarks/2026-09-02_attention_cpu.json

\- output JSON:

Observation:

\- ...

Hypothesis:

\- ...

Limitation:

\- ...

Deferred:

\- 9.3 H2D pipeline

\- 9.3 Full/Light reruns