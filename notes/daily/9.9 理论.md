1. 用加入时刻区分 Static/Dynamic/Continuous。

   Static 开始时加入，中途不可加入

   Dynamic 动态调整，中途不可加入

   Continuous ，中途可加入

2. 写 Scheduler iteration 六步。

   

3. 写 E2E 公式并定义 Concurrency。

   E2E ～= TTFT + (n-1) TOTP

   Concurrency 即每秒仍在处理的请求数

4. 解释 Prefix/refcount/COW。

prefix 是 KV cache 共用内存

refcount引用计数用于生命周期管理

COW，即拷贝在写后