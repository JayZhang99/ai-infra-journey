1. 写出 Baseline/Block 的 row 与 grid.x。

   Baseline row = BlockIdx.x * BlockDim.x + ThreadIdx.x grid.x = (rows + blockDim.x -1) / blockDim.x

   Block row = BlockIdx.x grid.x = rows

2. 逐字写出跨 Warp Reduction 的写者、Shared 槽、Barrier、读者。

   Warp Reduction

   写者 各warp lane 0， shared 槽：各warp lane0，Barrier 等待warp lane 0 写完，读者，其他warp

   写者 warp 0 lane 0， shared槽：warp 0 lane0，Barrier 等待 warp 0 lane 0 写完，读者

3. 50 repeats、100 launches/sample 时，p95 对什么样本求？

   p96是 50次repeat里面的95%数据，而并非 每一个repeat 的 100launch里的p96

4. TensorImpl 和 Storage 分别持有什么？

   TensorImpl 持有dtype、device, stride, layout等数据合同

   Storage 持有 数据保存的准确地点

5. TORCH_LIBRARY 与两个 TORCH_LIBRARY_IMPL 如何共同形成

Dispatch 表？

​	TORCH_LIBRARY 是注册接口

​	IMPL 是接口到具体Kernel 的映射

6. assert_close、opcheck、gradcheck 各验证什么？

​	assert_close 验证数值正确

​	opcheck 验证合同正确

​	gradcheck验证梯度生成正确





1. [2] rows=1000, threads=256：写 Baseline/Block 的 row 与 grid.x。

   Baseline row BlockIdx.x * blockDim.x + threadId.x grid.x = ceil(rows / threads)

   Block row BlockIdx.x grid.x = rows

2. [2] 用写者、Shared 槽、Barrier、读者解释跨 Warp Reduction。

   写者 各warp lane 0， shared 槽：各warp lane0，Barrier 等待warp lane 0 写完，读者，其他warp

   写者 warp 0 lane 0， shared槽：warp 0 lane0，Barrier 等待 warp 0 lane 0 写完，读者

3. [2] 定义 Transpose 的 50×100 p95 样本，并写 effective GB/s。

   p96是 50次repeat里面的95%数据，而并非 每一个repeat 的 100launch里的p96

   effective GB/s = 2 * width * height * sizeof(data) / median_ms * 1e6

4. [2] TensorImpl 与 Storage 各持有什么？transpose view 改了谁？

   TensorImpl 持有dtype、device, stride, layout等数据合同

   Storage 持有 数据保存的准确地点

   transpose view 改变了数据划分方式

5. [2] Schema、CPU IMPL、CUDA IMPL 如何形成一个 Operator 的

Dispatch 表？

​	Schema 根据 backend 选择CPU IMPL or CUDA IMPL

6. [2] assert_close、opcheck、gradcheck 各验证什么？

   assert_close 验证数值正确

   opcheck 验证 合同正确

   gradcheck 验证梯度生成正确

7. [2] 为什么同一个 torch.ops 调用能选择两个 Backend？

   因为Dispather根据Backend选择CPU IMPL or CUDA IMPL

8. [2] CUDAGuard 解决什么问题？单 GPU 为什么可能掩盖它？

   解决多GPU冲突问题，因为单GPU不保护也不会出现问题

9. [2] 为什么必须取得 PyTorch current stream，不能固定 default

stream？

​	保证顺序执行

10. [2] Launch Check 能发现什么？为什么仍需后续同步消费结果？

    发现调用参数是否正确，

    正确性验证必须等待同步消费结果

11. [2] CPU-only smoke 通过后，为什么 pytest 仍可能找不到

Operator？

​	CUDA还没有实现