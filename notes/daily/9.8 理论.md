1. 2 分：用加入时刻区分三种 Batching。

   固定Batching，组Batching时必须达到固定值

   动态Batching：组Batching可以动态大小，但执行释放资源同步

   批处理Batching：组Batching 和slot释放都是动态的

2. 2 分：写 Scheduler iteration 六步。

   xx->预估->分配->xx

3. 2 分：写 E2E 公式并定义 Concurrency。

   E2E ～= TTFT + （n-1） TTOT

4. 2 分：区分 token/KV/sequence slots。

   token 指用的算力

   KV Cache 指预估占用的内存

   sequence slots 则预估多少条sequence 

5. 2 分：为什么 Gap 要基于 busy union？

​	因为基于GPU Event 会有重叠算出来的Gap比实际值小





1. 2 分：Policy 与 Mechanism 各负责什么？

   policy 负责决定调度，Mechanism负责无误执行

1. 2 分：写 Allocator 七条不变量。

   Free block + busy block == total block

   free block 's ref == 0

   Block‘s id is legal

   free pool has no same id

   refcount >= 0

   free 和 allocated 集合不相交

2. 2 分：为什么 iteration 要事务化？

   成功后才改变状态，失败直接回退保存状态

3. 2 分：Prefix/Refcount/COW 的关系。

   Prefix 的值和Refcount一致

   当某任务完成，Refcount --

   新任务需要写时，需要复制Prefix，再释放引用

4. 2 分：FCFS 与 Decode-first 的指标取舍。

   FCFS 是公平，但可能导致ITL p99劣化

   decode-first 保证ITL，但可能导致用户觉得卡顿