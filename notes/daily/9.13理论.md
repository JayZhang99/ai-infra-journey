1. grid=2、block=4、n=18：写出 global_tid=5 处理的所有下标。

   5, 13

2. 写出 Baseline 与 Block 版本的 row 公式和 grid.x。

   Baseline row = threads grid.x = rows / threads

   Block row = block  grid.x = rows

3. 解释为何无数据线程仍要参加 Reduction 的 Barrier，并写出 sum/max 单

位元。

​	因为需要等待所有线程完成才能允许线程访问任意线程

​	sum 单位元 0

​	max 单位元 -inf

4. 写出 RMSNorm 完整公式，并说出 D 在 [rows, cols] 中是什么。

   sq = sum (x *x) /D

   inv = sqrt(1/(sq + eps))

   RMSNorm = inv * x * gamma

   D = cols

5. 解释 FP16 storage/FP32 accumulation；当前 JSON 的 p95 是什么样本的

p95。

​	storage 是指保存运输的数据类型为FP16

​	FP32 是指计算类型，因为reduction有大量累积，FP16可能出现精度丢失或者数据范围溢出

​	是Batch 200 的批次 p95





1. [2] 写出 RMSNorm 公式，说明 D 和 eps 的位置。

   MS = sum(x * x) /D

   inv = sqrt(1/(MS + eps))

   RMSNorm = inv * x * gamma

   D 是计算均方的分母

   eps 是计算inv 平方根内 相加MS

2. [2] 为什么 FP16 storage 常配 FP32 accumulation？

   因为 FP32精度更高，不容易溢出和精度丢失

3. [2] 比较 Baseline/Block 的 row 和 grid.x。

   Baseline row = threads grid.x = rows/ threads

   Block row = BlockIdx.x  grid.x = rows

4. [2] tile[32][33] 为什么能缓解 Bank conflict？

   

5. [2] Naive Transpose 哪个方向不合并？

   

6. [3] 用 RMSNorm 两个数字写纯 Observation。

   固定 rows 的情况下， cols越大，加速比越大

7. [3] 给出一个主假设和一个竞争解释。

   假设：cols 越大，threads 异步的优势越大，而syncthreads 的开销占比越小

   竞争解释：cols 越大 CPU 单线程处理等待时间过长

8. [4] 设计可推翻该假设的 Next test。

   固定 rows 多进行几组cols不同的对比实验，观察加速比

9. [3] 当前 p95 的样本单位是什么？

   bench，每bench的p95

10. [3] 写出 Transpose effective GB/s 公式。

    dataBytes / median_ms * 1e-6

11. [4] 列出今日三个工程产物及各自验收证据。

	1. 修复RMSNorm 测试实现，WSL 上Ctest通过	
	1. Transpose CUDA实现，WSL 上Ctest通过， bench 运行结果在benchmarks/transpose/runs下



