理解题：12 分，限时 18 分钟

1. 用数据库类比解释 Q、K、V 各自职责。（2）

   Q 是查询的key

   K 是数据索引

   V 是数据具体内容

2. 为什么 weights 与 scores shape 相同？（2）

   因为scores softmax 变换得到的weights

3. 为什么除以 sqrt(Dk)，请从方差和 Softmax 饱和解释。（2）

   方差除以sqrt(Dk) 可以保证不太大

   且不至于绝对值变大从而使得softmax退化为one-hot

4. Prefill 与 Decode 的 Q 长度和系统特征有什么不同？（2）

   Prefill Q是整个prompt长度

   Decode Q长度为1

   

5. 为什么 KV Cache 存 K/V 而不存历史 Q？（2）

   因为关心的就是历史K/V

6. TTFT 与 TPOT 分别主要覆盖哪一段时间？（2）

   TTFT 是prefill 得到第一个token输出的时间

   TPOP 是 decode生成每个token的时间





Shape 与场景题：18 分，限时 25

分钟

1. Q=[2,1,8]、K=[2,6,8]、V=[2,6,12]，写出 scores、weights、

output shape，并说明对应什么阶段。（3）

​	Q @ K^T

​	 scores [2,1,6]

​	mask 后 softmax 

​	weights[2,1,6]

​	weights @V

​	output shape [2,1,12]

2. 一个全 False mask 行为什么产生 NaN？今天的失败合同是什么？（

3）

​	因为 -inf-(-inf) == NaN

if not expanded_mask.any(dim=-1).all().item():

​            raise ValueError("mask contains a fully masked query row")

3. 完整 causal Attention 最后一个 token 与 cached decode 怎样

做数值一致性测试？（4）



4. 32 层、B=1、T=4096、D=4096、FP16 的 KV Cache 约多少 GiB？

写计算过程。（4）

5. 只有 full trace 有 gap，写 Observation、Hypothesis 和控制实验。

（4）