1. forward 正确与支持训练之间还缺什么能力？

   Autograd设置成功，Fake Implement符合预期，

   通过grad_check 和 op_check检查可证明

2. setup_context 的三个形参是什么，返回什么？

   ctx, inputs, output, 返回为空

3. scale_add backward 为什么返回二元组？

   因为forward 输入就是二元组 x,alpha

4. reduce_sum_fake 的 shape/dtype/device 是什么？

   shape 0-dim tensor

   dtype = x.dtype

   device = x.device

5. 画出 reduce_sum 的 forward/backward shape。

   forward shape x

   backward shape x

6.  load 为什么必须早于 runtime registrations？

   因为runtime registrations 时需要已经有算子的接口注册，所以必须先load




1. 画出 load 从 Python 到 Dispatcher 的六层链路。

   python-> compiler -> link -> load -> xx -> Dispatcher

2. compile error、link error、load error 各举一个信号。

   compile error que shao )

   link error  重复定义接口

   load error 

3. TORCH_EXTENSIONS_DIR 改变什么，不改变什么？

   改变当前环境 TORCH_EXTENSIONS 的工作路径

   不改变其他环境

4. 冷缓存、热缓存新进程、当前进程已加载有何区别？

   冷缓存 无缓存，无加载

   热缓存 有缓存、有加载

   已加载 无缓存、有加载

5. ExtensionVersioner 当前 hash 哪些主要输入？

   

6. 为什么 header 修改仍可能触发 Ninja 重编？

7. API 与 ABI 的区别是什么？

   API 是接口层级的， ABI 是构建层级的

8. 为什么升级 PyTorch 后旧动态库可能 undefined symbol？

   extensions 的meta data是旧pytorch

   需要重编

9. torch.version.cuda、nvcc、Driver 分别表示什么？

   Torch.version.cuda 是指 cuda version版本

   nvcc 是指 解释器

   Driver 是指 GPU driver

10. 为什么新 pytest 进程必须再次 load + register？

    因为新的进程没有加载算子

    所以必须重新Load + register

11. nm -C、ldd/otool、dispatch_dump_table 分别证明什么？

    nm -C 证明编译

    ldd 证明链接依赖

    Dispatch_dump_table 证明python implementation已注册

12. 怎样证明 warm build 真的复用了构建产物？

    SHA256 和时间都一致