1. 为什么 ONNX 不是 nn.Module + state_dict 的二进制快照？

   因为ONNX 不只是model 的信息

• 2. 画出 ModelProto、GraphProto、NodeProto、TensorProto、

ValueInfoProto 的关系，并说明 Node 如何接线。

​	GraphProto -> ModelProto -> NodeProto ->TensorProto -> ValueInfoProto

通过name 接线

• 3. CUDA EP 在 available providers 中，为什么仍可能有 CPU 节点与

H2D/D2H？

​	可能出现数据传输

• 4. Session startup 与 steady-state run 分别包含什么？为什么不能放

在同一 latency sample？

​	start up 包含加载模型、内存 

​	steady-state run 即稳态 推理一次

​	start up 应该是一个一次性动作，而非每次run 都需要，所以不能放在同一 letency sample

• 5. TensorRT Engine 与 ExecutionContext 分别保存什么？

min/opt/max 又是什么？

​	Engine 保存每一次execution 的参数、硬件

​	ExecutionContext 则是每一次具体Execution 的上下文

​	min 是最小shape

​	opt 是最多shape

​	max 是最大shape

• 6. 写出 TinyFFN parity 的合法 shape、非法输入、dtype 与 atol/rtol。

​	合法shape [B,T, 8]

​	非法输入 last D !- 8

​	dtype torch.float32

​	

1. Basic、Extended、Layout optimization 的主要区别和大致时机是

什么？

​	

• 2. Online 与 offline graph optimization 的启动成本和可移植性差异

是什么？

​	Online 每次都启动，成本高，移植性好

​	offline 只优化一次，成本低， 移植性差

• 3. intra-op 与 inter-op 分别并行什么？为什么 TinyFFN 不一定适合

ORT_PARALLEL？

​	intra-op 输入

​	inter-op 推理

​	因为 TinyFFN 模型小，计算占比小， ORT_PARALLEL 可能导致负向优化

• 4. 为什么 14 个原始 ONNX Node 不等于 14 个最终 kernel？

​	因为Runtime可能进行融合

• 5. ORT profile 中 cat/name/dur/args.provider/args.op_name 分

别回答什么？

​	cat : 

​	name:

​	dur: kernel 时间

​	args.provider: kernel执行者

​	args.op_name:执行算子名

• 6. 为什么带 profiling 的 Node dur 不能替代正式 Benchmark

median/p95？

​	因为Node dur 带了profiling 的开销

​	正式benchmark 需要关掉profile 重新跑